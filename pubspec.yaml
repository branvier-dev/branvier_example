name: branvier_teste
description: "Projeto de Estoque de Produtos da Branvier"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  #core
  provider: ^6.1.1
  go_router: ^13.1.0

  #services
  # dio: ^4.0.0

  #ui
  flutter_async: ^0.6.3
  formx: ^0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
